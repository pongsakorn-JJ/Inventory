# วิธีติดตั้งไฟล์ที่แก้ไข

ไฟล์ในซิปนี้คือ "โค้ดที่เปลี่ยนแปลง" เท่านั้น (ไม่มี node_modules)
ให้คัดลอกทับไฟล์เดิมในโปรเจกต์ Inventory ของคุณ แล้วรันตามปกติ

## 1) ฐานข้อมูล (ต้องทำก่อน)
รันไฟล์ `backend/migration.sql` กับฐานข้อมูล `ip_std6730202688` เพื่อเพิ่มคอลัมน์
`location` และ `stock_quantity` ในตาราง `products` (โค้ด backend ใหม่ใช้ 2 คอลัมน์นี้)

## 2) Backend
คัดลอก `backend/server.js` ทับไฟล์เดิม แล้วรัน `npm run dev` (หรือ `npm start`) ตามปกติ
มี endpoint ใหม่ 3 ตัว:
- `PATCH /api/products/:id/stock` — ปรับสต็อกเร็ว ๆ (body: `{ delta }`) เฉพาะแอดมิน
- `POST /api/sales` — บันทึกการขาย 1 รายการ (body: `{ productId, quantity }`) ลดสต็อกอัตโนมัติและสร้างใบเสร็จ
- `GET /api/dashboard` — สรุปสต็อกและยอดขายสำหรับหน้าแดชบอร์ด

## 3) Frontend
คัดลอกโฟลเดอร์ `frontend/src` ทับของเดิมทั้งหมด (ลบไฟล์เก่าออกก่อน เพราะมีการลบ
`cart.tsx`, `favorites.tsx`, `brand.tsx` และแทนที่ด้วย `products.tsx`)
จากนั้นรัน `npx expo start --web` หรือ `npx expo start` ตามปกติ — ไม่ต้อง `npm install` ใหม่
เพราะไม่ได้เพิ่ม dependency ใหม่

## สิ่งที่เปลี่ยนไปจากเดิม
- ตัดฟีเจอร์ตะกร้า/รายการโปรดของลูกค้าออก เพราะแอปนี้ปรับเป็น "ระบบจัดการสต็อกสินค้า"
  สำหรับพนักงาน/แอดมิน ตามที่ขอ (Add/Edit/Delete/Search + Login/Logout + Location + Dashboard)
- เพิ่มฟิลด์ **ตำแหน่งจัดเก็บ (location)** และ **จำนวนสต็อก (stock quantity)** ในสินค้าทุกชิ้น
- หน้า **แดชบอร์ด** (`/`): การ์ดสรุปจำนวนสินค้า/สต็อกรวม/สินค้าใกล้หมด/ยอดขายรวม,
  ตารางสรุปยอดขายต่อสินค้า, สต็อกตามตำแหน่งจัดเก็บ, รายการขายล่าสุด
- หน้า **สินค้า** (`/products`): ค้นหา + กรองหมวดหมู่, ปุ่ม "บันทึกการขาย" (ลดสต็อก),
  ปุ่ม +/- ปรับสต็อกด่วน (แอดมิน), แก้ไข/ลบ (แอดมิน) — กล่องรูปสินค้าเป็นขนาดคงที่พร้อม
  `resizeMode="contain"` ทำให้รูปสินค้าทุกสัดส่วนแสดงพอดีกรอบ ไม่ถูกครอบตัดหรือยืดเบี้ยว
- หน้า **เข้าสู่ระบบ/ออกจากระบบ** (`/login`): ตัดการเลือก role ออก สมาชิกใหม่ที่สมัคร
  จะได้สิทธิ์ "พนักงาน" อัตโนมัติ ส่วนสิทธิ์ "แอดมิน" ต้องตั้งในฐานข้อมูลเอง (คอลัมน์ role)
- สินค้าที่มี role "พนักงาน" (user) ยังค้นหา/ดูสต็อก/บันทึกการขายได้ตามปกติ
  แต่เพิ่ม/แก้ไข/ลบสินค้าได้เฉพาะ "แอดมิน" เท่านั้น (สิทธิ์เดิมของระบบ)

## ถ้าต้องการเพิ่มเติม
บอกได้เลย เช่น พิมพ์รายงานสต็อก/ยอดขายเป็น PDF, แจ้งเตือนสต็อกต่ำผ่านอีเมล,
กราฟยอดขายรายวัน/เดือน, ระบบสแกนบาร์โค้ด ฯลฯ
